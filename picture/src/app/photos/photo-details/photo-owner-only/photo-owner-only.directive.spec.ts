import { PhotoOwnerOnlyDirective } from './photo-owner-only.directive';

describe('PhotOwnerOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoOwnerOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
